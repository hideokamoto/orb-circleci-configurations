version: 2.1
orbs:
  # Your orb will be automatically injected here during the pipeline.
  # Reference your orb's jobs and commands below as they will exist when built.
  orb-tools: circleci/orb-tools@12.3
  # The orb definition is intentionally not included here. It will be injected into the pipeline.
  configurations: {}

# Use this tag to ensure test jobs always run,
# even though the downstream publish job will only run on release tags.
filters: &filters
  tags:
    only: /.*/

# Filter for release tags.
release-filters: &release-filters
  branches:
    ignore: /.*/
  tags:
    only: /^v[0-9]+\.[0-9]+\.[0-9]+$/

jobs:
  # Exercise the orb's executors directly, so a packaging or parameter
  # regression in either one fails test-deploy before publish.
  test_base_pinned_executor:
    executor: configurations/base_pinned
    steps:
      - checkout
      - run:
          name: Confirm base_pinned resolves to a cimg/base image
          command: cat /etc/os-release | head -2
      - run:
          # cimg/base does not ship the `circleci` CLI on PATH. What the
          # deploy-marker commands actually depend on is the build-agent
          # CLI CircleCI injects into every job at /usr/bin/circleci (see
          # the migration source's .circleci/continue-config.yml and
          # workflows/deployment/cloudflare-workers.yaml, both of which
          # call it by full path on cimg-family images in production).
          # Check that full path instead of PATH resolution, plus a
          # baseline tool (git) that base_pinned jobs also rely on.
          name: Confirm build-agent circleci CLI and git are present
          command: |
            test -x /usr/bin/circleci
            git --version
  test_trivy_executor:
    executor: configurations/trivy
    steps:
      - checkout
      - run:
          name: Confirm trivy resolves to a working Trivy image
          command: trivy --version
  # Exercises send_sbom_to_port's happy path end-to-end (parameter wiring,
  # circleci env subst, curl invocation) without ever sending a real
  # request: a stub curl on PATH records the invocation instead of
  # dialing out. The exit-1 "webhook url unset" path is covered by BATS.
  test_send_sbom_to_port:
    executor: configurations/base_pinned
    environment:
      TEST_PORT_WEBHOOK_URL: "http://127.0.0.1:8080/webhook"
    steps:
      - checkout
      - run:
          name: Install a stub curl and a fixture SBOM file
          command: |
            mkdir -p /tmp/stub-bin
            cat > /tmp/stub-bin/curl <<'CURL_STUB'
            #!/usr/bin/env bash
            printf '%s\n' "$@" > /tmp/curl-calls.log
            exit 0
            CURL_STUB
            chmod +x /tmp/stub-bin/curl
            echo 'export PATH="/tmp/stub-bin:$PATH"' >> "$BASH_ENV"
            echo '{"bomFormat":"CycloneDX","components":[]}' > /tmp/test-sbom.cdx.json
      - configurations/send_sbom_to_port:
          webhook_url_env: TEST_PORT_WEBHOOK_URL
          sbom_file: /tmp/test-sbom.cdx.json
          retries: 1
          max_time: 5
      - run:
          name: Confirm the stub curl received the expected POST
          command: |
            test -f /tmp/curl-calls.log
            grep -q -- '--data-binary' /tmp/curl-calls.log
            grep -q '@/tmp/test-sbom.cdx.json' /tmp/curl-calls.log
            grep -q -- "${TEST_PORT_WEBHOOK_URL}" /tmp/curl-calls.log
workflows:
  test-deploy:
    jobs:
      # Make sure to include "filters: *filters" in every test job you want to run as part of your deployment.
      # Test your orb's commands in a custom job and test your orb's jobs directly as a part of this workflow.
      - test_base_pinned_executor:
          filters: *filters
      - test_trivy_executor:
          filters: *filters
      - test_send_sbom_to_port:
          filters: *filters
      # The orb must be re-packed for publishing, and saved to the workspace.
      - orb-tools/pack:
          filters: *release-filters
      - orb-tools/publish:
          orb_name: hideokamoto/configurations
          vcs_type: << pipeline.project.type >>
          pub_type: production
          # Ensure this job requires all test jobs and the pack job.
          requires:
            - orb-tools/pack
            - test_base_pinned_executor
            - test_trivy_executor
            - test_send_sbom_to_port
          context: orb-publishing
          filters: *release-filters
